<!-- <%= form_tag '/', method: :get %>
<%= select_tag "music[alpha]", options_for_select(('A'..'Z').to_a) %>
<%= submit_tag 'search' %> -->


<% @musics.each do |music| %>
  <div class="col-md-6">
    <div class="card">
      <h1 class="music-title"><%= link_to "#{music.title.titleize}", music %></h1>
      <h2><%= music.composer.titleize %></h2>
    </div>
  </div>
<% end %>
